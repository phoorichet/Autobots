<%= form_for(@rnc) do |f| %>
  <% if @rnc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rnc.errors.count, "error") %> prohibited this rnc from being saved:</h2>

      <ul>
      <% @rnc.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :sgsn_id %><br>
    <%= f.number_field :sgsn_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
